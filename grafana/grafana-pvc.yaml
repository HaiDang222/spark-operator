apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ''
  volumeMode: Filesystem
  volumeName: grafana-pv
  resources:
    requests:
      storage: 1Gi